fn main() {
    // Agent binary — will be integrated into the main orca binary.
    // Kept as a separate crate for modularity during development.
    // See orca-cli `orca server` / `orca agent` for the real entry points.
    println!("orca-agent: use `orca server` or `orca agent` instead");
}
